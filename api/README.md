# API - Gather Data from an API

This project uses Python and the REST API (https://jsonplaceholder.typicode.com)
to get TODO list progress for a given employee ID.

## Usage
```bash
./0-gather_data_from_an_API.py <employee_id>

